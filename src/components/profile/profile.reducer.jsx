const initState = {
    name: 'Rishi Suresh'
}

const userDetails = (state = initState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default userDetails;